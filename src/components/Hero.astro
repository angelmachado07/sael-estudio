<section class="hero" aria-labelledby="hero-title">
  <div class="container hero-container">
    <div class="hero-content">
      <p class="hero-eyebrow">
        Identidad, diseño y acompañamiento creativo
      </p>

      <h1 id="hero-title">
        Diseño con intención.
        <span>Marcas con alma.</span>
      </h1>

      <p class="hero-description">
        Acompañamos marcas y proyectos a construir una identidad clara,
        coherente y sensible, capaz de comunicar quiénes son y hacia dónde
        quieren crecer.
      </p>

      <div class="hero-actions">
        <a class="button button-primary" href="#contacto">
          Conversemos
        </a>

        <a class="button button-secondary" href="#servicios">
          Ver servicios
        </a>
      </div>
    </div>

    <div class="hero-visual" aria-hidden="true">
      <div class="visual-frame">
        <div class="visual-symbol">
          <span class="symbol-line"></span>
          <span class="symbol-dot"></span>
          <span class="symbol-line"></span>
        </div>

        <div class="visual-copy">
          <span>Sael</span>
          <small>Estudio creativo</small>
        </div>

        <div class="visual-index">01</div>
      </div>
    </div>
  </div>

  <div class="hero-bottom">
    <div class="container hero-bottom-container">
      <span>Identidad visual</span>
      <span>Diseño gráfico</span>
      <span>Presencia digital</span>
      <span>Acompañamiento creativo</span>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid var(--color-border);
  }

  .hero-container {
    min-height: calc(100vh - 82px);
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(340px, 0.92fr);
    align-items: center;
    gap: clamp(56px, 8vw, 120px);
    padding-block: clamp(72px, 10vw, 140px);
  }

  .hero-content {
    max-width: 760px;
  }

  .hero-eyebrow {
    margin-bottom: 28px;
    color: var(--color-olive);
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 820px;
    margin-bottom: 32px;
    font-size: clamp(4rem, 7vw, 7.4rem);
    letter-spacing: -0.055em;
  }

  h1 span {
    display: block;
    color: var(--color-moss);
    font-style: italic;
  }

  .hero-description {
    max-width: 610px;
    margin-bottom: 38px;
    color: rgba(44, 46, 38, 0.76);
    font-size: clamp(1rem, 1.4vw, 1.18rem);
    line-height: 1.75;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
  }

  .button {
    min-height: 50px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding-inline: 24px;
    border: 1px solid var(--color-moss);
    border-radius: 999px;
    font-size: 0.88rem;
    font-weight: 600;
    transition:
      background-color var(--transition-fast),
      color var(--transition-fast),
      transform var(--transition-fast);
  }

  .button:hover {
    transform: translateY(-2px);
  }

  .button-primary {
    background-color: var(--color-moss);
    color: var(--color-cream);
  }

  .button-primary:hover {
    background-color: var(--color-espresso);
  }

  .button-secondary {
    background-color: transparent;
    color: var(--color-moss);
  }

  .button-secondary:hover {
    background-color: var(--color-stone);
  }

  .hero-visual {
    display: flex;
    justify-content: center;
  }

  .visual-frame {
    position: relative;
    width: min(100%, 470px);
    aspect-ratio: 4 / 5;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(79, 90, 69, 0.55);
    border-radius: 36px;
    background:
      linear-gradient(
        145deg,
        rgba(255, 255, 255, 0.26),
        rgba(216, 210, 196, 0.38)
      );
  }

  .visual-frame::before {
    content: "";
    position: absolute;
    inset: 18px;
    border: 1px solid rgba(79, 90, 69, 0.18);
    border-radius: 24px;
  }

  .visual-symbol {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 14px;
  }

  .symbol-line {
    width: 1px;
    height: 90px;
    background-color: var(--color-moss);
  }

  .symbol-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background-color: var(--color-moss);
  }

  .visual-copy {
    position: absolute;
    left: 34px;
    bottom: 34px;
    display: flex;
    flex-direction: column;
  }

  .visual-copy span {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    line-height: 1;
  }

  .visual-copy small {
    margin-top: 8px;
    color: var(--color-olive);
    font-size: 0.66rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }

  .visual-index {
    position: absolute;
    top: 32px;
    right: 32px;
    color: var(--color-olive);
    font-size: 0.72rem;
    letter-spacing: 0.12em;
  }

  .hero-bottom {
    border-top: 1px solid var(--color-border);
  }

  .hero-bottom-container {
    min-height: 72px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    align-items: center;
    gap: 24px;
  }

  .hero-bottom span {
    color: var(--color-olive);
    font-size: 0.74rem;
    letter-spacing: 0.1em;
    text-align: center;
    text-transform: uppercase;
  }

  @media (max-width: 1024px) {
    .hero-container {
      min-height: auto;
      grid-template-columns: 1fr;
      gap: 72px;
    }

    .hero-content {
      max-width: 850px;
    }

    .hero-visual {
      justify-content: flex-start;
    }

    .visual-frame {
      width: min(100%, 620px);
      aspect-ratio: 5 / 4;
    }

    .hero-bottom-container {
      grid-template-columns: repeat(2, 1fr);
      padding-block: 24px;
    }
  }

  @media (max-width: 640px) {
    .hero-container {
      padding-block: 64px 72px;
      gap: 48px;
    }

    .hero-eyebrow {
      margin-bottom: 22px;
      font-size: 0.68rem;
      line-height: 1.6;
    }

    h1 {
      margin-bottom: 26px;
      font-size: clamp(3.3rem, 16vw, 5rem);
    }

    .hero-description {
      margin-bottom: 30px;
      line-height: 1.65;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .button {
      width: 100%;
    }

    .visual-frame {
      aspect-ratio: 4 / 5;
      border-radius: 26px;
    }

    .visual-frame::before {
      inset: 12px;
      border-radius: 18px;
    }

    .symbol-line {
      height: 64px;
    }

    .visual-copy {
      left: 24px;
      bottom: 24px;
    }

    .visual-copy span {
      font-size: 2rem;
    }

    .visual-index {
      top: 24px;
      right: 24px;
    }

    .hero-bottom-container {
      grid-template-columns: 1fr;
      gap: 14px;
      padding-block: 24px;
    }
  }
</style>