<section id="inicio" class="hero" aria-labelledby="hero-title">
  <div class="hero-surface" aria-hidden="true"></div>

  <div class="container hero-layout">
    <div class="hero-copy">
      <p class="hero-kicker" data-reveal>Sael Estudio <span>Diseño + soluciones</span></p>

      <h1 id="hero-title">
        <span class="hero-line" data-reveal>Diseñamos</span>
        <span class="hero-line hero-line-accent" data-reveal style="--reveal-delay: 100ms">
          soluciones<span aria-hidden="true">.</span>
        </span>
      </h1>

      <div class="hero-message" data-reveal style="--reveal-delay: 190ms">
        <p>
          Para que marcas y proyectos puedan verse mejor, comunicar mejor y
          funcionar mejor.
        </p>

        <div class="hero-actions">
          <a class="hero-primary" href="#contacto">
            Contanos qué necesitás
            <span aria-hidden="true">↗</span>
          </a>
          <a class="hero-secondary" href="#servicios">Explorar soluciones</a>
        </div>
      </div>
    </div>

    <div class="solution-canvas" data-parallax data-tilt aria-hidden="true">
      <div class="canvas-grid"></div>

      <svg class="solution-route" viewBox="0 0 560 640" fill="none">
        <path d="M67 109H225V246H397V371H293V528H493" />
        <circle cx="67" cy="109" r="5" />
        <circle cx="225" cy="246" r="5" />
        <circle cx="397" cy="371" r="5" />
        <circle cx="293" cy="528" r="5" />
        <circle cx="493" cy="528" r="5" />
      </svg>

      <div class="solution-plane plane-brand">
        <span class="plane-index">01</span>
        <span class="plane-mark"><i></i><b></b><i></i></span>
        <strong>Marca</strong>
      </div>

      <div class="solution-plane plane-communication">
        <span class="plane-index">02</span>
        <div class="plane-composition"><i></i><i></i><i></i></div>
        <strong>Comunicación</strong>
      </div>

      <div class="solution-plane plane-digital">
        <span class="plane-index">03</span>
        <div class="plane-interface"><i></i><i></i><i></i><i></i></div>
        <strong>Tecnología</strong>
      </div>

      <div class="solution-core">
        <span>S</span>
        <i></i>
      </div>

      <div class="canvas-caption">Una necesidad<br />una solución propia</div>
    </div>

    <div class="hero-signature" data-reveal style="--reveal-delay: 280ms">
      <span class="signature-symbol" aria-hidden="true"><i></i><b></b><i></i></span>
      <p>Diseño con intención.<br /><em>Marcas con alma.</em></p>
    </div>

    <a class="hero-scroll" href="#sobre-sael" aria-label="Continuar hacia Sobre Sael">
      <span></span>
      <i></i>
    </a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: calc(100svh - var(--header-height));
    overflow: hidden;
    background:
      linear-gradient(90deg, transparent 49.94%, rgba(79, 90, 69, 0.13) 50%, transparent 50.06%),
      linear-gradient(rgba(243, 239, 231, 0.18), rgba(243, 239, 231, 0.18)),
      var(--color-stone);
    border-bottom: 1px solid rgba(79, 90, 69, 0.24);
  }

  .hero-surface {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 12% 20%, rgba(243, 239, 231, 0.74), transparent 28%),
      radial-gradient(circle at 82% 76%, rgba(107, 116, 92, 0.18), transparent 32%);
    pointer-events: none;
  }

  .hero-layout {
    position: relative;
    min-height: calc(100svh - var(--header-height));
    display: grid;
    grid-template-columns: minmax(0, 1.03fr) minmax(390px, 0.97fr);
    grid-template-rows: 1fr auto;
    gap: 28px clamp(34px, 6vw, 96px);
    align-items: center;
    padding-block: clamp(40px, 5vw, 76px) 30px;
  }

  .hero-copy {
    position: relative;
    z-index: 3;
    align-self: center;
  }

  .hero-kicker {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-bottom: clamp(34px, 5vw, 72px);
    color: var(--color-espresso);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .hero-kicker::before {
    content: "";
    width: 34px;
    height: 1px;
    background: currentColor;
  }

  .hero-kicker span {
    color: rgba(44, 46, 38, 0.52);
  }

  h1 {
    margin: 0;
    color: var(--color-espresso);
    font-size: clamp(6rem, 10.5vw, 11.2rem);
    line-height: 0.72;
    letter-spacing: -0.065em;
  }

  .hero-line {
    display: block;
    width: max-content;
  }

  .hero-line-accent {
    position: relative;
    z-index: 4;
    margin-left: clamp(20px, 7vw, 104px);
    color: var(--color-moss);
    font-style: italic;
  }

  .hero-line-accent > span {
    color: var(--color-espresso);
  }

  .hero-message {
    display: grid;
    grid-template-columns: minmax(220px, 360px) auto;
    gap: clamp(28px, 4vw, 64px);
    align-items: end;
    margin-top: clamp(50px, 7vw, 100px);
    margin-left: clamp(0px, 7vw, 104px);
  }

  .hero-message > p {
    margin: 0;
    color: rgba(44, 46, 38, 0.76);
    font-size: clamp(1rem, 1.25vw, 1.15rem);
    line-height: 1.65;
  }

  .hero-actions {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 13px;
  }

  .hero-primary {
    display: inline-flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
    min-height: 56px;
    padding: 0 18px 0 22px;
    border-radius: 2px;
    background: var(--color-espresso);
    color: var(--color-cream);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.045em;
    text-transform: uppercase;
    transition: transform var(--transition-normal), background-color var(--transition-normal);
  }

  .hero-primary:hover {
    background: var(--color-moss);
    transform: translateY(-4px);
  }

  .hero-secondary {
    padding-bottom: 4px;
    border-bottom: 1px solid rgba(44, 46, 38, 0.42);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .solution-canvas {
    position: relative;
    z-index: 2;
    justify-self: end;
    width: min(100%, 610px);
    aspect-ratio: 0.88;
    overflow: hidden;
    border: 1px solid rgba(44, 46, 38, 0.28);
    background: rgba(243, 239, 231, 0.48);
    box-shadow: 0 40px 100px rgba(44, 46, 38, 0.12);
    transform:
      translate3d(0, var(--parallax-offset, 0), 0)
      perspective(900px)
      rotateX(var(--tilt-y, 0deg))
      rotateY(var(--tilt-x, 0deg));
    transform-style: preserve-3d;
    transition: transform 300ms ease-out;
  }

  .canvas-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(79, 90, 69, 0.12) 1px, transparent 1px),
      linear-gradient(90deg, rgba(79, 90, 69, 0.12) 1px, transparent 1px);
    background-size: 20% 20%;
  }

  .solution-route {
    position: absolute;
    inset: 4%;
    width: 92%;
    height: 92%;
    stroke: rgba(79, 90, 69, 0.5);
    stroke-width: 1;
  }

  .solution-route path {
    stroke-dasharray: 1300;
    stroke-dashoffset: 1300;
    animation: route-draw 2.4s var(--ease-out) 500ms forwards;
  }

  .solution-route circle {
    fill: var(--color-moss);
  }

  .solution-plane {
    position: absolute;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 18px;
    border: 1px solid rgba(44, 46, 38, 0.25);
    box-shadow: 0 18px 45px rgba(44, 46, 38, 0.1);
    transition: transform 800ms var(--ease-out), box-shadow 800ms var(--ease-out);
  }

  .solution-plane strong,
  .plane-index {
    font-family: var(--font-body);
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .plane-index { opacity: 0.52; }

  .plane-brand {
    top: 8%;
    left: 8%;
    width: 42%;
    height: 31%;
    background: var(--color-cream);
    transform: translateZ(34px) rotate(-3deg);
  }

  .plane-communication {
    top: 35%;
    right: 7%;
    width: 46%;
    height: 30%;
    background: var(--color-olive);
    color: var(--color-cream);
    transform: translateZ(58px) rotate(2deg);
  }

  .plane-digital {
    bottom: 7%;
    left: 13%;
    width: 50%;
    height: 27%;
    background: var(--color-espresso);
    color: var(--color-cream);
    transform: translateZ(82px) rotate(-1.5deg);
  }

  .solution-canvas:hover .plane-brand { transform: translate3d(-8px, -8px, 55px) rotate(-5deg); }
  .solution-canvas:hover .plane-communication { transform: translate3d(8px, -4px, 82px) rotate(4deg); }
  .solution-canvas:hover .plane-digital { transform: translate3d(-4px, 10px, 108px) rotate(-3deg); }

  .plane-mark {
    display: flex;
    align-items: center;
    gap: 8px;
    margin: auto;
  }

  .plane-mark i { width: 46px; height: 1px; background: var(--color-moss); }
  .plane-mark b { width: 9px; height: 9px; border-radius: 50%; background: var(--color-moss); }

  .plane-composition {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 6px;
    height: 52%;
    margin: auto 0;
  }

  .plane-composition i { border: 1px solid rgba(243, 239, 231, 0.52); }
  .plane-composition i:nth-child(2) { grid-row: span 2; border-radius: 50% 50% 0 0; }

  .plane-interface {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 5px;
    height: 48%;
    margin: auto 0;
    padding: 8px;
    border: 1px solid rgba(243, 239, 231, 0.35);
  }

  .plane-interface i { background: rgba(243, 239, 231, 0.16); }
  .plane-interface i:first-child { grid-column: 1 / -1; }

  .solution-core {
    position: absolute;
    top: 44%;
    left: 39%;
    z-index: 6;
    width: 88px;
    aspect-ratio: 1;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--color-stone);
    border: 1px solid rgba(44, 46, 38, 0.3);
    box-shadow: 0 14px 35px rgba(44, 46, 38, 0.18);
  }

  .solution-core span {
    font-family: var(--font-heading);
    font-size: 3.3rem;
    font-style: italic;
  }

  .solution-core i {
    position: absolute;
    top: 8px;
    right: 12px;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--color-moss);
    animation: core-pulse 2.6s ease-in-out infinite;
  }

  .canvas-caption {
    position: absolute;
    right: 18px;
    bottom: 18px;
    color: rgba(44, 46, 38, 0.56);
    font-size: 0.57rem;
    font-weight: 600;
    line-height: 1.5;
    letter-spacing: 0.1em;
    text-align: right;
    text-transform: uppercase;
  }

  .hero-signature {
    align-self: end;
    display: flex;
    align-items: center;
    gap: 18px;
  }

  .signature-symbol { display: flex; align-items: center; gap: 6px; }
  .signature-symbol i { width: 24px; height: 1px; background: var(--color-moss); }
  .signature-symbol b { width: 6px; height: 6px; border-radius: 50%; background: var(--color-moss); }

  .hero-signature p {
    margin: 0;
    font-family: var(--font-heading);
    font-size: 1.2rem;
    line-height: 1.05;
  }

  .hero-signature em { color: var(--color-moss); }

  .hero-scroll {
    justify-self: end;
    align-self: end;
    display: flex;
    align-items: center;
    gap: 0;
    width: 94px;
    height: 20px;
  }

  .hero-scroll span { width: 100%; height: 1px; background: var(--color-moss); }
  .hero-scroll i { width: 8px; height: 8px; flex: 0 0 auto; border-radius: 50%; background: var(--color-moss); }

  @keyframes route-draw { to { stroke-dashoffset: 0; } }
  @keyframes core-pulse { 50% { transform: scale(1.8); opacity: 0.35; } }

  @media (max-width: 1100px) {
    .hero-layout { grid-template-columns: 1fr 0.8fr; }
    h1 { font-size: clamp(5rem, 11vw, 8rem); }
    .hero-message { grid-template-columns: 1fr; margin-left: 0; }
    .solution-canvas { width: min(100%, 500px); }
  }

  @media (max-width: 780px) {
    .hero { min-height: auto; }
    .hero-layout {
      min-height: auto;
      grid-template-columns: 1fr;
      grid-template-rows: auto auto auto;
      gap: 34px;
      padding-block: 34px 26px;
    }

    .hero-kicker { margin-bottom: 48px; }
    h1 { font-size: clamp(4.4rem, 19.5vw, 7.6rem); line-height: 0.75; }
    .hero-line-accent { margin-left: 6vw; }
    .hero-message { max-width: 560px; margin-top: 48px; }
    .hero-actions { flex-direction: row; align-items: center; flex-wrap: wrap; }

    .solution-canvas {
      justify-self: center;
      width: min(100%, 560px);
      aspect-ratio: 0.92;
      margin-top: 8px;
    }

    .hero-signature { grid-row: 3; }
    .hero-scroll { display: none; }
  }

  @media (max-width: 480px) {
    .hero-layout { gap: 28px; }
    .hero-kicker { justify-content: space-between; gap: 8px; font-size: 0.58rem; }
    .hero-kicker::before { display: none; }
    h1 { font-size: clamp(4rem, 20vw, 5.2rem); }
    .hero-message > p { font-size: 1rem; }
    .hero-actions { align-items: flex-start; flex-direction: column; }
    .solution-canvas { aspect-ratio: 0.9; }
    .solution-plane { padding: 12px; }
    .solution-plane strong, .plane-index { font-size: 0.48rem; }
    .solution-core { width: 64px; }
    .solution-core span { font-size: 2.45rem; }
    .plane-mark i { width: 26px; }
    .canvas-caption { display: none; }
  }
</style>
