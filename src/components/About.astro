<section id="sobre-sael" class="about" aria-labelledby="about-title">
  <div class="container about-shell">
    <div class="about-intro">
      <p class="section-label" data-reveal>Descubrir Sael</p>
      <div class="about-principle">
        <p>No partimos de una receta fija. Escuchamos el contexto y construimos la respuesta que cada proyecto necesita.</p>
        <div class="about-mark" aria-hidden="true">
          <svg viewBox="0 0 320 138" focusable="false">
            <path class="route-base" d="M18 70H92C126 70 126 25 160 25H302" />
            <path class="route-base" d="M92 70C126 70 126 114 160 114H302" />
            <path class="route-active" d="M18 70H92C126 70 126 25 160 25H302" />
            <circle cx="18" cy="70" r="5" /><circle cx="92" cy="70" r="5" />
            <circle cx="160" cy="25" r="6" /><circle cx="160" cy="114" r="5" />
            <circle class="route-end" cx="302" cy="25" r="7" />
          </svg>
        </div>
      </div>
    </div>

    <h2 id="about-title">
      <span data-reveal="mask">No trabajamos</span>
      <span data-reveal="mask" style="--reveal-delay:70ms">desde una fórmula.</span>
    </h2>
  </div>

  <div class="creative-flow" aria-hidden="true">
    <div class="creative-flow-track">
      <div class="creative-flow-set">
        <span>Pensar</span><i>·</i><span>Explorar</span><i>·</i><span>Diseñar</span><i>·</i><span>Probar</span><i>·</i><span>Hacer</span><i>·</i><span>Ajustar</span><i>·</i>
      </div>
      <div class="creative-flow-set">
        <span>Pensar</span><i>·</i><span>Explorar</span><i>·</i><span>Diseñar</span><i>·</i><span>Probar</span><i>·</i><span>Hacer</span><i>·</i><span>Ajustar</span><i>·</i>
      </div>
    </div>
  </div>
</section>

<style>
  .about { position:relative; z-index:3; overflow:hidden; padding-top:clamp(110px,13vw,190px); background:var(--color-moss); color:var(--color-cream); }
  .about-shell { display:grid; grid-template-columns:minmax(220px,.42fr) minmax(0,1fr); gap:clamp(60px,8vw,125px); }
  .about-intro { display:flex; flex-direction:column; min-height:clamp(360px,42vw,560px); }
  .about-intro .section-label { color:rgba(243,239,231,.78); }
  .about-principle { margin-top:clamp(58px,8vw,108px); }
  .about-principle > p { max-width:360px; margin:0; color:rgba(243,239,231,.84); font-size:clamp(1.05rem,1.15vw,1.2rem); line-height:1.65; }
  .about-mark { width:100%; max-width:360px; margin-top:34px; border-block:1px solid rgba(243,239,231,.22); }
  .about-mark svg { display:block; width:100%; height:auto; overflow:visible; }
  .about-mark path { fill:none; stroke:rgba(243,239,231,.35); stroke-width:1.5; }
  .about-mark .route-active { stroke:var(--color-stone); stroke-width:2; stroke-dasharray:340; stroke-dashoffset:340; animation:route-draw 4.8s var(--ease-in-out) infinite; }
  .about-mark circle { fill:var(--color-moss); stroke:rgba(243,239,231,.58); stroke-width:1.5; }
  .about-mark .route-end { fill:var(--color-stone); stroke:var(--color-stone); }
  h2 { min-width:0; margin:0; font-size:clamp(5.1rem,9.2vw,9.4rem); font-weight:500; line-height:.82; letter-spacing:-.07em; }
  h2 span { display:block; width:max-content; max-width:100%; padding-bottom:.12em; margin-bottom:-.12em; }
  .creative-flow { width:100%; margin-top:clamp(90px,12vw,170px); overflow:hidden; border-block:1px solid rgba(79,90,69,.22); background:var(--color-cream); color:var(--color-moss); }
  .creative-flow-track { width:max-content; display:flex; animation:creative-flow 30s linear infinite; will-change:transform; }
  .creative-flow-set { display:flex; align-items:center; flex:none; gap:clamp(12px,1.5vw,22px); padding:clamp(10px,1.25vw,14px) clamp(14px,1.7vw,24px); font-family:var(--font-heading); font-size:clamp(1.4rem,2.2vw,2.4rem); font-weight:500; line-height:1; letter-spacing:-.035em; white-space:nowrap; }
  .creative-flow-set i { color:var(--color-olive); font-family:var(--font-body); font-size:.55em; font-style:normal; }
  @keyframes creative-flow { from { transform:translateX(-50%); } to { transform:translateX(0); } }
  @keyframes route-draw { 45%,100% { stroke-dashoffset:0; } }
  @media (max-width:800px) { .about-shell { grid-template-columns:1fr; } .about-intro { min-height:280px; } h2 { font-size:clamp(4.8rem,15vw,7.2rem); } }
  @media (max-width:560px) { .about-shell { gap:64px; } .about-intro { min-height:0; } .about-principle { margin-top:44px; } .about-mark { margin-top:26px; } h2 { font-size:clamp(4.05rem,18vw,5.2rem); } .creative-flow { margin-top:82px; } .creative-flow-set { gap:12px; padding:10px 14px; font-size:clamp(1.35rem,6vw,1.65rem); } }
  @media (prefers-reduced-motion:reduce) { .creative-flow-track,.about-mark .route-active { animation:none; } .about-mark .route-active { stroke-dashoffset:0; } }
</style>
